'use strict';

angular.
  module('addComment').
  component('addComment', {
    templateUrl: 'addcomment/add-comment.template.html',
    controller: ['$scope', '$http', function AddCommentController($scope, $http) {
      var self = this;
      

      }]
   });